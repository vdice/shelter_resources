require('spec_helper')

describe('the admin functionality', {:type => :feature}) do

    describe('CRUD for shelters') do
      # still needed?
    end
    describe('CRUD for resources') do
      # still needed?
    end
end
